import {createContext,useContext,useState,useCallback} from 'react'; import {api} from '../api/client';
const C=createContext(null); const save=d=>{localStorage.setItem('token',d.token);localStorage.setItem('user',JSON.stringify(d.user));};
export function AuthProvider({children}){const [user,setUser]=useState(()=>{try{return JSON.parse(localStorage.getItem('user'))}catch{return null}}); const apply=useCallback(d=>{save(d);setUser(d.user)},[]); const login=useCallback(async(e,p)=>apply(await api.login(e,p)),[apply]); const register=useCallback(async(e,p,n)=>apply(await api.register(e,p,n)),[apply]); const google=useCallback(async(c)=>apply(await api.google(c)),[apply]); const logout=()=>{localStorage.clear();setUser(null)}; return <C.Provider value={{user,login,register,google,logout}}>{children}</C.Provider>}; export const useAuth=()=>useContext(C);
