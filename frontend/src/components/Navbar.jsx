import{Link}from'react-router-dom';import{useAuth}from'../context/AuthContext';export default function Navbar(){const{user,logout}=useAuth();if(!user)return null;return <nav><Link to="/">Outbox Scheduler</Link><div><span>{user.name||user.email}</span><button onClick={logout}>Logout</button></div></nav>}
