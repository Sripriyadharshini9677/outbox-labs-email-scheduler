const fmt=x=>x?new Date(x).toLocaleString():'—';export default function EmailTable({emails,showCancel,onCancel}){if(!emails.length)return <div className="empty-state">Nothing here yet.</div>;return <div className="table-wrap"><table><thead><tr><th>To</th><th>Subject</th><th>Scheduled</th><th>Status</th><th>Sent</th><th>Action</th></tr></thead><tbody>{emails.map(e=><tr key={e.id}><td>{e.to_address}</td><td>{e.subject}</td><td>{fmt(e.scheduled_at)}</td><td><span className={`badge ${e.status}`}>{e.status}</span></td><td>{fmt(e.sent_at)}</td><td>{e.preview_url&&<a className="link-btn" href={e.preview_url} target="_blank" rel="noreferrer">Preview</a>}{showCancel&&e.status==='scheduled'&&<button className="link-btn danger" onClick={()=>onCancel(e.id)}>Cancel</button>}</td></tr>)}</tbody></table></div>}
