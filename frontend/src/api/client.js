const API_URL=import.meta.env.VITE_API_URL||'http://localhost:4000/api';
function token(){return localStorage.getItem('token')}
async function request(path,{method='GET',body,form=false}={}){const headers={}; const t=token(); if(t) headers.Authorization=`Bearer ${t}`; if(!form) headers['Content-Type']='application/json'; const r=await fetch(`${API_URL}${path}`,{method,headers,body:form?body:body?JSON.stringify(body):undefined}); const d=await r.json().catch(()=>({})); if(!r.ok) throw new Error(d.error||`Request failed (${r.status})`); return d;}
export const api={register:(email,password,name)=>request('/auth/register',{method:'POST',body:{email,password,name}}),login:(email,password)=>request('/auth/login',{method:'POST',body:{email,password}}),google:(credential)=>request('/auth/google',{method:'POST',body:{credential}}),listEmails:(status)=>request(`/emails${status?`?status=${status}`:''}`),scheduleEmail:(form)=>request('/emails',{method:'POST',body:form,form:true}),cancelEmail:id=>request(`/emails/${id}`,{method:'DELETE'})};
