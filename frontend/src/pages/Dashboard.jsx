import{useEffect,useState,useCallback}from'react';import{Link}from'react-router-dom';import{api}from'../api/client';import EmailTable from'../components/EmailTable';export default function Dashboard(){const[emails,setEmails]=useState([]),[loading,setLoading]=useState(true),[error,setError]=useState('');const load=useCallback(async()=>{try{setEmails(await api.listEmails())}catch(e){setError(e.message)}finally{setLoading(false)}},[]);useEffect(()=>{load();const i=setInterval(load,5000);return()=>clearInterval(i)},[load]);const cancel=async id=>{try{await api.cancelEmail(id);load()}catch(e){setError(e.message)}};const scheduled=emails.filter(e=>e.status==='scheduled'||e.status==='sending'),sent=emails.filter(e=>e.status==='sent'),other=emails.filter(e=>['failed','cancelled'].includes(e.status));return <div className="container"><div className="topbar"><div><h1>Dashboard</h1><p className="subtitle">Scheduled and sent emails</p></div><Link to="/compose"><button className="primary compact">+ Compose</button></Link></div>{error&&<div className="error-text">{error}</div>}{loading?<p className="subtitle">Loading…</p>:<><section><h2>Scheduled</h2><EmailTable emails={scheduled} showCancel onCancel={cancel}/></section><section><h2>Sent</h2><EmailTable emails={sent}/></section>{other.length>0&&<section><h2>Failed / Cancelled</h2><EmailTable emails={other}/></section>}</>}</div>}
