import {useState} from 'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';export default function Register(){const{register}=useAuth(),nav=useNavigate();const[name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');async function s(e){e.preventDefault();try{await register(email,password,name);nav('/')}catch(x){setError(x.message)}}return <div className="auth-shell"><form className="card narrow" onSubmit={s}><h1>Create account</h1><label>Name</label><input value={name} onChange={e=>setName(e.target.value)}/><label>Email</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><label>Password</label><input type="password" minLength="8" value={password} onChange={e=>setPassword(e.target.value)} required/>{error&&<div className="error-text">{error}</div>}<button className="primary">Create account</button><div className="hint-text">Already registered? <Link to="/login">Sign in</Link></div></form></div>}
